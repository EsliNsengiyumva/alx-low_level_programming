#include <stdio.h>

/**
 *
 * Display numbers from 0 to 9 with separation of comma and space
 * 
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
		if (i != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
